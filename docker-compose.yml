services:
  gdbuddy:
    image: ghcr.io/bare-metal-edu/gdbuddy:latest
    environment:
      RUNNER_ORG: https://github.com/bare-metal-edu
      GITHUB_PAT: ${GITHUB_PAT}
    privileged: true
    devices:
      - "/dev/tty.usbmodem1101:/dev/ttyACM0"